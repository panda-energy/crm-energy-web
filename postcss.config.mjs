// PostCSS config for Tailwind CSS v4.
// Tailwind 4 ya no usa el preset clásico; todo va por su plugin de PostCSS.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
